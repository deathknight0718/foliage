[package]
name = "foliage-ai-native"
version = "0.1.0"
edition = "2021"

[lib]
name = "foliageai"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
candle-core = { git = "https://github.com/huggingface/candle.git", rev = "0.6.0", version = "0.6.0", features = ["cuda"] }
